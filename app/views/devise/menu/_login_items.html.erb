<% if friend_signed_in? %>
  <li><%= link_to('Logout', destroy_friend_session_path) %></li>
<% else %>
  <li><%= link_to('Login', new_friend_session_path)  %></li>
<% end %>
<li>User: 
  <% if current_friend %>
    <%= current_friend.name %>
  <% else %>
    (not logged in)
  <% end %>
</li>